// Re-export types from central types file for backward compatibility
export * from '../types';
import type { Post, PrayerRequest } from '../types';

export const MOCK_POSTS: Post[] = [];
export const MOCK_PRAYERS: PrayerRequest[] = [];
